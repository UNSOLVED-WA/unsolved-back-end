name: distribute.yml
on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  distribute:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy bmtown
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USERNAME }}
          key: ${{ secrets.SERVER_KEY }}
          port: ${{ secrets.SERVER_PORT }}
          script: |
            cd ~/unsolved-back-end
            git pull
            ./gradlew build -x test
            java -jar ./build/libs/unsolved-wa-0.0.1-SNAPSHOT.jar
