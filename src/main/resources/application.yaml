spring:
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    generate-ddl: false
  datasource:
    url: jdbc:mysql://localhost:3306/unsolved_wa?useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 1234

sample:
  value: default
